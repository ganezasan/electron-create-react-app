{
  "name": "hoge",
  "version": "0.1.0",
  "homepage": "./",
  "main": "src/electron-starter.js",
  "private": true,
  "dependencies": {
    "electron": "^1.7.12",
    "foreman": "^2.0.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "electron-packager": "^10.1.2",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "electron": "node_modules/.bin/electron .",
    "dev": "nf start -p 3000",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "package": "electron-packager . hoge --platform=linux --arch=armv7l --overwrite --prune=true --out=release"
  }
}
